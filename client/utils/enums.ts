export enum UserRoles {
  ADMIN = 'admin',
  DECLARANT = 'declarant',
  MANAGER = 'manager',
}

export enum OrderStatuses {
  NEW = 'NEW',
  INSPECTION = 'INSPECTION',
  DOCUMENT = 'DOCUMENT',
  REGISTRATION = 'REGISTRATION',
  FINISHED = 'FINISHED',
}

export enum DocumenTypes {
  DECLARANT = 'declarant',
  INCOMING = 'incoming',
  DECORATED = 'decorated',
  SERVICE = 'service',
}
